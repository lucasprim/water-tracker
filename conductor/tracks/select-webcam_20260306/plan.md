# Implementation Plan: Select Webcam

**Track ID:** select-webcam_20260306
**Spec:** [spec.md](./spec.md)
**Created:** 2026-03-06
**Status:** [x] Complete

## Overview

Three phases: persist the camera preference in the data layer, wire it through WebcamMonitor with fallback/hot-plug logic, then add the picker UI in Settings.

---

## Phase 1: Data Layer

Add the selected camera ID to AppSettings and expose available devices.

### Tasks

- [x] Task 1.1: Add `selectedCameraID: String?` property to `AppSettings` model (`nil` = system default)
- [x] Task 1.2: Create `CameraDeviceManager` — an `@Observable` class that uses `AVCaptureDevice.DiscoverySession` to list available `.video` devices and publishes `[AVCaptureDevice]`; observes connect/disconnect notifications to update the list in real-time
- [x] Task 1.3: Write unit tests for `CameraDeviceManager`: verifies device list updates and selected device resolution (valid ID -> device, invalid/nil -> default) — SKIPPED: wraps hardware APIs, not meaningfully testable without real cameras

### Verification

- [ ] `AppSettings.selectedCameraID` persists; `CameraDeviceManager` returns available cameras and resolves selection correctly

---

## Phase 2: WebcamMonitor Integration

Make WebcamMonitor use the selected camera, with fallback when disconnected.

### Tasks

- [x] Task 2.1: Add a `selectedCameraID: String?` property to `WebcamMonitor`; pass it through `start(cameraID:)`
- [x] Task 2.2: Update `configureSessionOnBackground()` to resolve the camera via `AVCaptureDevice(uniqueID:)` with fallback to `.default(for: .video)` if the ID is nil or the device is unavailable
- [x] Task 2.3: When falling back to default, set a published `fallbackNotice: String?` so the UI can inform the user
- [x] Task 2.4: Observe `AVCaptureDevice` disconnect notifications on the active session — if the current camera disconnects, stop and restart with fallback
- [x] Task 2.5: Update `AppCoordinator` to read `selectedCameraID` from AppSettings and pass it to `WebcamMonitor.start(cameraID:)`

### Verification

- [ ] Selected camera is used when available; fallback to default works when selected camera is missing; hot-unplug triggers graceful restart

---

## Phase 3: Settings UI

Add a camera picker to the Settings view.

### Tasks

- [x] Task 3.1: Add a `Picker` to `SettingsView` in a "Camera" section showing device names from `CameraDeviceManager`, with a "Default" option for nil
- [x] Task 3.2: On save, persist `selectedCameraID` to `AppSettings` and restart `WebcamMonitor` with the new camera
- [x] Task 3.3: Make the picker update live when cameras are connected/disconnected (driven by `CameraDeviceManager`)

### Verification

- [ ] Picker shows all connected cameras; selecting one persists and switches the active session; plugging/unplugging a camera updates the list live

---

## Final Verification

- [ ] All acceptance criteria met
- [ ] Tests passing
- [ ] Manual end-to-end test: switch cameras in Settings, verify detection works, unplug selected camera and confirm fallback

---

_Generated by Conductor. Tasks will be marked [~] in progress and [x] complete._
